fn main() {
    let x = 10;
    let y = 5;

    // let x: f64 = 9.0;
    // let y: u32 = 5;

    // cannot add `u32` to `f64` no implementation for `f64 + u32`

    let sum = x + y;
    let difference = x - y;
    let quotient = x / y;
    let product = x * y;
    let remainder = x % y;

	println!("{}", sum);
	println!("{}", difference);
	println!("{}", quotient);
	println!("{}", product);
	println!("{}", remainder);




}
